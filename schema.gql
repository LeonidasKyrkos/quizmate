# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Mutation {
  addQuiz(newQuizData: NewQuizInput!): Quiz!
  removeQuiz(id: String!): Boolean!
  login(password: String!, email: String!): ID!
  registerUser(password: String!, email: String!): ID!
}

input NewQuizInput {
  title: String!
  description: String
}

type Query {
  quiz(id: String!): Quiz!
  allQuizzes: [Quiz!]!
  userQuizzes(user: String!): [Quiz!]!
  allUsers: [User!]!
}

type Question {
  id: ID!
  question: String!
  answer: String!
  multipleChoice: Boolean!
  choices: [String!]
}

type Quiz {
  id: ID!
  title: String!
  description: String
  questions: [Question]
}

type Subscription {
  quizAdded: Quiz!
}

type User {
  id: ID!
  email: String!
}
